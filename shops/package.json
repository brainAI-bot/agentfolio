{
  "name": "@agentfolio/shops-boundary",
  "version": "0.0.0-private",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check:boundary": "node scripts/validate-boundary.mjs",
    "check:services": "node scripts/wait-for-services.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
