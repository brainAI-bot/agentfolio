# Makings PR Watch List

Source authority: `brand-vault/brainAI/CURRENT`. Legacy AgentFolio identifiers and pages remain in this repository, but new public positioning follows the Makings pre-launch board.

Scope: organic listening, owned-account replies, GitHub/Moltbook responses, and in-thread engagement for Makings and clearly labelled legacy AgentFolio surfaces. This board is separate from scheduled marketing posts.

## Channels

| Channel family | Owned stream | Topic search | Public communities/accounts |
| --- | --- | --- | --- |
| X | `@makingsHQ` mentions, replies, quote posts; board-flagged repost opportunities from `@brainAI_` | `Makings`, `makingsHQ`, `versioned skills`, `agent adapters`, `data packs`, `x402 agents`, plus `AgentFolio` for legacy support | Agent builders, MCP, x402, AI-agent tooling, and software-marketplace threads |
| GitHub | Issues, PRs, discussions, mentions in `brainAI-bot/agentfolio` and related repositories | `Makings`, `AgentFolio` (legacy), `agentfolio-mcp-server`, `versioned skills`, `x402` | Relevant open-source issues and discussions where Makings or a legacy AgentFolio surface has a direct product answer |
| Moltbook | Makings and legacy AgentFolio mentions, comments, and relevant build/discovery threads | `Makings`, `versioned skills`, `agent adapters`, `data packs`, `x402 agents` | Founder, build, and agent-builder threads |

## Streams

- Own-account mentions and repo/community mentions are first priority.
- Topic search is second priority and must map to Makings, versioned agent products, MCP, x402, or a concrete legacy AgentFolio support question.
- One reply per thread. Add concrete product context or stay silent.
- Do not duplicate `@brainAI_` originals. `@makingsHQ` reposts only items explicitly allowed by `brand-vault/brainAI/CURRENT`.

## Validation Receipts

- 2026-06-28 repo/account validation: repository `brainAI-bot/agentfolio` exists and current head read through GitHub clone; `brand-kit/BRAND-GUIDE.md` present; brand board path present at `brand-vault/brainAI/marketing-v3`.
- Known capability receipt: GitHub and Moltbook proven for AgentFolio/SATP per HQ task acceptance criteria; cite exact permalink/receipt id from HQ when executing.
